# `kg_rag_demo` 改写任务清单

## 1. 总目标

- 把 `kg_rag_demo` 改写为新系统的底层能力。
- 只保留数据导入 pipeline 和检索 Agent pipeline。
- 原有数据必须继续可用，不重新构建数据集。
- 旧 memory 不保留、不迁移。
- 删除冗余实验性查询方法，保证检索速度。

## 2. 必须复用的数据

- `kg_rag_demo/data/data/single_chunks.json`
- `kg_rag_demo/data/data/multi_chunks.json`
- `kg_rag_demo/data/data/qa_pairs.json`
- `kg_rag_demo/data/data/subgraphs.json`
- `kg_rag_demo/data/data/test_dataset.json`
- 已有 Chroma 向量数据。
- 已有 Neo4j 图谱数据。

## 3. 不复用的内容

- Streamlit 页面。
- 旧问答入口。
- 旧 short/long memory。
- `workdir/memory/short_term_memory.jsonl`
- `workdir/memory/long_term_memory.jsonl`
- 过多 runtime config 开关。
- 多套实验性 query rewrite。
- 默认重型 reranker。
- 默认复杂图谱多跳。

## 4. 可复用代码

- `parsers.py`：资料解析。
- `chunking.py`：文档切分。
- `vector_store.py`：Chroma 和 embedding。
- `graph_store.py`：Neo4j 存储和查询。
- `models.py`：基础数据结构。
- `pipeline.py`：ingest、断点续传、并发抽取思路。

## 5. 数据兼容任务

- 读取旧 JSON 数据。
- 建立旧 ID 到新 ID 的映射。
- 保留旧 `chunk_id` 引用。
- 保留旧 subgraph 节点和边。
- 适配旧 Chroma collection。
- 适配旧 Neo4j label/relation。
- 新增 `LegacyDataMapping`。
- 支持旧数据只读模式。
- 新数据写入新系统表。

## 6. 数据导入 Pipeline

实现流程：

```text
文件上传/旧数据读取
  -> 元数据登记
  -> 文档解析
  -> 文本清洗
  -> 文档切分
  -> Chunk 入库
  -> embedding
  -> 向量库写入
  -> 知识点/关系抽取
  -> 图谱写入
  -> 导入报告
```

必须支持：

- PDF
- PPT/PPTX
- Word/DOCX
- Markdown
- TXT
- 代码文件
- 增量导入
- 断点续传
- 失败记录
- 进度追踪

## 7. 检索 Agent Pipeline

统一为一条固定 pipeline：

```text
query + course_id + 当前阅读位置
  -> Query Normalize
  -> Scope Filter
  -> Vector Search + Keyword Search + KG Search
  -> Merge + Deduplicate
  -> 轻量 Rerank
  -> Context Pack
  -> RetrievalResult
```

默认策略：

- TopK 小。
- 图谱默认 1 跳。
- 需要时最多 2 跳。
- 默认不启用重型 reranker。
- 默认不启用复杂 query rewrite。
- 检索结果按 `chunk_id` 去重。
- 优先返回当前文档和当前章节相关结果。

## 8. 检索 Agent 输出

输出结构必须包含：

- `query`
- `course_id`
- `document_scope`
- `chunks`
- `kg_context`
- `citations`
- `score`
- `source_type`
- `knowledge_points`

检索 Agent 不负责：

- 最终回答。
- 用户画像。
- 普通讲解。
- 学习评估。
- 资源生成。

## 9. 记忆处理

- 不迁移旧 memory。
- 不读取旧 short memory。
- 不读取旧 long memory。
- 不用旧问答历史生成 `user_profile`。
- 新系统重新实现 `me`、`user_profile`、`working_memory`、`conversation_memory`、`memory`。

## 10. 性能要求

- 删除冗余检索路径。
- 删除不必要的实验开关。
- 检索、KG 查询、用户画像读取允许并行。
- 常用课程和知识点做缓存。
- embedding 不重复计算。
- 旧向量可用时不重建。
- 只有模型维度不兼容时才提供可选重建命令。

## 11. 验证任务

- 用 `test_dataset.json` 做检索回归测试。
- 验证旧 `single_chunks.json` 可用。
- 验证旧 `multi_chunks.json` 可用。
- 验证旧 `qa_pairs.json` 可用。
- 验证旧 `subgraphs.json` 可用。
- 验证旧 Chroma 数据可接入。
- 验证旧 Neo4j 图谱可查询。
- 验证旧 memory 不进入新系统。

# `kg_rag_demo` 复用与优化方案

## 1. 目标

`kg_rag_demo` 是之前做的 KG + RAG 问答系统，里面已有资料解析、向量库、知识图谱和查询方法。新系统不直接照搬，而是把它改写成两个稳定模块：

1. 数据导入 pipeline。
2. 检索 Agent pipeline。

同时保留可用的数据和经验，删除实验性、冗余、影响速度的检索分支。旧项目 memory 不需要保留，新系统记忆按主 Agent 架构重新实现。

重要约束：必须保证原来的数据能继续使用，不能要求重新构建数据集。改写时优先做兼容层、迁移脚本和索引适配，复用 `kg_rag_demo` 现有数据、chunk、图谱和向量索引。旧 memory 文件不复用、不迁移。

## 2. 可复用内容

可以复用：

- `data/data/` 下已有测试数据和处理后的数据。
- 原有 `single_chunks.json`、`multi_chunks.json`、`qa_pairs.json`、`subgraphs.json`、`test_dataset.json` 等数据文件。
- 已经构建过的向量索引和图谱数据，能复用就复用，不能复用时只做自动迁移，不让用户重新整理数据集。
- `parsers.py`：PDF、Word、PPT、图片等资料解析思路。
- `chunking.py`：文档切分逻辑。
- `vector_store.py`：Chroma + embedding 的向量检索能力。
- `graph_store.py`：Neo4j 图谱存储与查询能力。
- `models.py`：`ParsedDocument`、`ChunkRecord`、`RelationEdge` 等基础数据结构。
- `pipeline.py` 中的 ingest 流程、断点续传、并发图谱抽取思路。

不直接复用：

- Streamlit 页面。
- 旧的问答入口。
- 过多实验性检索开关。
- 多套互相叠加的查询方法。
- 旧的 short/long memory 文件和记忆机制。

## 3. 数据导入 Pipeline

新系统需要把 `kg_rag_demo` 的 ingest 改成课程资料导入服务。

注意：数据导入 pipeline 不是要求重新做数据集，而是支持两种入口：

1. 兼容导入已有数据。
   - 直接读取 `kg_rag_demo/data/data/` 中已有 JSON 数据。
   - 将旧 chunk、qa、subgraph 数据映射到新系统的数据模型。
   - 保留原始 ID 或建立 ID 映射表，避免引用失效。

2. 新资料上传导入。
   - 只在后续新增课件或资料时使用。
   - 不影响已有数据继续使用。

目标流程：

```text
上传课程资料
  ↓
文件保存与元数据登记
  ↓
文档解析
  ↓
文本清洗
  ↓
文档切分
  ↓
写入 Document / Chunk 表
  ↓
生成 embedding
  ↓
写入向量库
  ↓
抽取知识点与关系
  ↓
写入轻量知识图谱
  ↓
生成导入报告
```

### 需要保留的能力

- 兼容已有数据文件，不要求重新构建数据集。
- 支持 PDF、PPT/PPTX、Word/DOCX、Markdown、TXT、代码文件。
- 支持断点续传，已处理 chunk 不重复处理。
- 支持导入进度追踪。
- 支持失败文件和失败 chunk 记录。
- 支持增量导入。

### 需要简化的地方

- 导入阶段只保留一条标准流程，不保留多套实验入口。
- 图谱抽取只做课程学习需要的关系，不做过泛的实体关系抽取。
- 第一版图谱可以不用复杂 Neo4j 查询，先用关系型表表达节点和边。

### 推荐数据模型

- `Course`
- `CourseDocument`
- `DocumentChunk`
- `KnowledgePoint`
- `KnowledgeEdge`
- `IngestJob`
- `IngestJobItem`
- `LegacyDataMapping`

`LegacyDataMapping` 用于记录旧数据 ID 和新系统 ID 的对应关系，例如旧 `chunk_id` 到新 `DocumentChunk.id`，旧 subgraph 节点到新 `KnowledgePoint.id`。

## 3.1 旧数据兼容方案

必须优先保证以下旧数据可用：

- `kg_rag_demo/data/data/single_chunks.json`
- `kg_rag_demo/data/data/multi_chunks.json`
- `kg_rag_demo/data/data/qa_pairs.json`
- `kg_rag_demo/data/data/subgraphs.json`
- `kg_rag_demo/data/data/test_dataset.json`
- 已有 Chroma 向量数据。
- 已有 Neo4j 图谱数据。

兼容策略：

1. JSON 数据优先直接读取。
   - 不重新生成。
   - 只做字段映射和格式校验。

2. 向量库优先复用已有 collection。
   - 如果 embedding 模型和 collection 结构一致，直接接入。
   - 如果新系统字段不同，建立适配层，不立即重算 embedding。
   - 只有在模型或维度不兼容时，才提供可选重建命令。

3. Neo4j 图谱优先复用已有节点和边。
   - 新系统查询层适配旧 label/relation。
   - 后续再逐步迁移到新的 `KnowledgePoint` / `KnowledgeEdge` 结构。

4. 旧 memory 不迁移。
   - 不读取 `workdir/memory/short_term_memory.jsonl`。
   - 不读取 `workdir/memory/long_term_memory.jsonl`。
   - 新系统上线后重新生成自己的 `conversation_memory`、`memory` 和 `user_profile`。

5. 保留只读兼容模式。
   - 第一版可以不修改旧数据，只读使用。
   - 新产生的数据写入新系统。
   - 等系统稳定后再做全量迁移。

## 4. 检索 Agent Pipeline

原 `kg_rag_demo` 的查询方法偏实验性，参数和开关太多。新系统中检索应作为一个稳定、快速、可测试的 pipeline，封装为检索 Agent。

### 检索 Agent 职责

检索 Agent 统一负责：

- 资料检索。
- 知识图谱查询。
- 混合检索。
- 引用片段打包。
- 返回结构化上下文给主 Agent 或资源生成 Agent。

检索 Agent 不负责：

- 最终回答用户。
- 用户画像维护。
- 普通讲解。
- 学习评估。
- 资源生成。

### 推荐查询流程

```text
query + course_id + 当前阅读位置
  ↓
Query Normalize
  ↓
Scope Filter
  ↓
并行检索
  ├─ Vector Search
  ├─ Keyword Search
  └─ KG Search
  ↓
Merge + Deduplicate
  ↓
轻量 Rerank
  ↓
Context Pack
  ↓
返回 RetrievalResult
```

### 查询流程说明

1. `Query Normalize`
   - 清理问题。
   - 结合当前资料位置补全 query。
   - 不做复杂多轮 query rewrite，除非主 Agent 明确要求。

2. `Scope Filter`
   - 限定课程、章节、文档、页码、用户权限。
   - 这是必须步骤，不能让 Agent 绕过。

3. `Vector Search`
   - 默认 TopK 小一些，例如 6-8。
   - 主要用于语义相似片段。

4. `Keyword Search`
   - 用于精确术语、公式、专有名词。
   - 第一版可以用数据库 LIKE/全文索引，不必引入复杂 BM25。

5. `KG Search`
   - 查询知识点、先修关系、相关章节、关联题目。
   - 不做复杂多跳，默认 1 跳，必要时 2 跳。

6. `Merge + Deduplicate`
   - 按 `chunk_id`、`document_id`、知识点去重。
   - 避免同一段资料重复进入上下文。

7. `轻量 Rerank`
   - 优先使用规则重排：当前文档、标题命中、关键词命中、知识点相关度、向量分数。
   - 第一版不默认使用重型 reranker，保证速度。

8. `Context Pack`
   - 输出给 Agent 的不是散乱 hits，而是结构化上下文。
   - 必须包含来源、页码、章节、chunk_id、知识点、文本片段。

## 5. 需要删除或弱化的冗余能力

`kg_rag_demo` 中以下能力不作为新系统默认路径：

- 过多 runtime config 开关。
- 多套 query rewrite。
- 图谱实体同义词扩展默认关闭。
- 图谱多跳默认关闭或限制为 1 跳。
- 图谱结果复杂打分弱化。
- 重型 reranker 默认关闭。
- evidence compression 不作为检索默认步骤，可作为上下文超长时的后处理。
- 问答 pipeline 不再直接生成最终 answer，最终回答由主 Agent 完成。

目标是：检索 Agent 快速、稳定、结构化，不追求实验性召回最大化。

## 6. 检索结果结构

检索 Agent 返回统一结构：

```json
{
  "query": "监督学习为什么要划分训练集和测试集",
  "course_id": "course_ai",
  "document_scope": {
    "document_id": "doc_001",
    "page": 12
  },
  "chunks": [
    {
      "chunk_id": "chunk_001",
      "document_id": "doc_001",
      "title": "监督学习流程",
      "text": "……",
      "page": 12,
      "score": 0.86,
      "source_type": "vector",
      "knowledge_points": ["监督学习", "模型评估"]
    }
  ],
  "kg_context": [
    {
      "source": "训练集",
      "relation": "used_for",
      "target": "模型训练",
      "evidence": "训练集用于拟合模型参数"
    }
  ],
  "citations": [
    {
      "document_id": "doc_001",
      "page": 12,
      "chunk_id": "chunk_001"
    }
  ]
}
```

## 7. 与主 Agent 的关系

检索 Agent 是专家 Agent，但不直接对用户说话。

主 Agent 调用流程：

```text
主 Agent 判断需要资料依据
  ↓
Planner 创建检索任务
  ↓
检索 Agent 执行固定 pipeline
  ↓
返回 RetrievalResult
  ↓
主 Agent 基于结果继续对话或交给资源生成 Agent
```

主 Agent 可以决定是否检索、检索什么，但不能绕过检索 pipeline 直接操作向量库或图谱。

## 8. 记忆机制处理

`kg_rag_demo` 的旧 memory 不需要保留，也不需要迁移。新系统按主 Agent 架构重新实现记忆：

- `me`
- `user_profile`
- `working_memory`
- `conversation_memory`
- `memory`

处理原则：

- 不读取旧 `short_term_memory.jsonl`。
- 不读取旧 `long_term_memory.jsonl`。
- 不从旧问答历史生成用户画像。
- 不把旧 memory 作为新系统长期记忆来源。
- 新系统上线后，从真实用户对话、阅读行为和练习行为重新生成记忆。

### 用户画像

与学生学习相关的长期信息写入 `user_profile`：

- 最近在学什么。
- 薄弱知识点。
- 常错题型或概念。
- 掌握度。
- 阅读行为。
- 练习表现。
- 资源偏好。
- 讲解偏好。

### 压缩机制

新系统压缩方式：

- 对话按天摘要。
- 多天摘要合并为阶段总结。
- 相似 memory 合并。
- 过期或低置信度 memory 降权。
- 被新证据推翻的 memory 归档。

## 9. 性能优化原则

为了保证速度，新检索默认策略：

- 默认 TopK 小。
- 默认图谱 1 跳。
- 默认不启用重型 reranker。
- 默认不启用复杂 query rewrite。
- 检索、KG 查询、用户画像读取可以并行。
- 对常用课程资料和知识点做缓存。
- 对上传资料增量处理，不重复 embedding。
- 对检索结果按 `chunk_id` 去重。

## 10. 改写优先级

第一阶段：数据导入

- 复用 parser、chunking、vector store。
- 建立课程资料导入任务。
- 完成 PDF/PPT/Word/Markdown/TXT/代码文件解析。
- 完成 chunk 入库和向量入库。

第二阶段：检索 Agent

- 实现固定检索 pipeline。
- 返回统一 `RetrievalResult`。
- 接入主 Agent/Planner。

第三阶段：轻量知识图谱

- 从 chunk 中抽取知识点。
- 建立知识点和章节关系。
- 支持 1 跳查询。

第四阶段：新记忆机制实现

- 不迁移旧 memory。
- 实现 `me`、`user_profile`、`working_memory`、`conversation_memory`、`memory`。
- 将新产生的学习状态类信息写入 `user_profile`。

第五阶段：旧数据兼容验证

- 用原有 `test_dataset.json` 跑检索回归测试。
- 检查旧 chunk 是否能被检索。
- 检查旧 subgraph 是否能被查询。
- 检查旧 qa pairs 是否能用于评估回答质量。

## 11. 结论

`kg_rag_demo` 不作为最终系统直接使用，而作为底层能力来源。

最终改写目标：

- 数据导入 pipeline：负责资料上传、解析、切分、向量入库、图谱构建。
- 检索 Agent pipeline：负责快速、稳定、结构化检索。
- 记忆机制：不复用旧 memory，按新系统主 Agent 记忆架构重新实现。

核心原则是：少实验开关、少冗余路径、保证速度、保证引用、保证能和主 Agent/Planner/资源生成 Agent 对接。

同时必须保证：原来的数据继续可用，不要求重新构建数据集。

