# 个性化学习多智能体系统执行清单

## 1. 产品目标

- 重做当前“一键生成 Demo”，改成真实学习平台。
- 核心功能只保留两条主线：用户阅读资料、用户和多 Agent 交互学习。
- 所有回答、讲解、题目、路径和资源生成都基于课程资料库。

## 2. 输入体系

- 做课程资料输入：PDF、PPT/PPTX、Word/DOCX、Markdown、TXT、代码文件、题库。
- 做用户对话输入：学习目标、当前问题、反馈、偏好、资料选中文本。
- 资料输入用于构建课程数据库、向量库、知识图谱。
- 对话输入用于驱动主 Agent、更新 `user_profile`、调用 Planner 和专家 Agent。

## 3. 前端页面

- 首页/课程入口页：课程列表、最近学习、学习进度、继续学习推荐。
- 课程资料页：课程目录、章节、课件、文档、代码案例、题库入口。
- 资料阅读页：阅读单份资料，支持搜索、高亮、笔记、收藏、进度。
- 多 Agent 学习页：聊天、解释、追问、诊断、调用资源生成。
- 学习路径页：阶段计划、推荐资源、完成状态、下一步任务。
- 练习与测评页：题库、练习记录、错题分析、掌握度变化。
- 资源库页：生成的讲解文档、笔记、思维导图、练习题、代码案例、视频脚本。
- 智能体工作台页：Planner 任务、专家 Agent 状态、审校结果、重试记录。

## 4. 前端能力

- 支持 Markdown 渲染。
- 支持代码块和语法高亮。
- 支持数学公式。
- 支持 Mermaid 图。
- 支持表格、图片、链接。
- 支持 PDF 阅读。
- 支持 PPT/PPTX 阅读或分页预览。
- 支持 Word/DOCX 阅读或转换后阅读。
- 支持 Markdown/TXT/代码文件阅读。
- 支持选中文本后向 Agent 提问。
- 阅读器向主 Agent 传递 `course_id`、`document_id`、`page`、`section`、`selected_text`、`nearby_chunks`、`user_note`。

## 5. 数据与知识库

- 建立课程数据模型：`Course`、`CourseDocument`、`DocumentChunk`。
- 建立知识图谱模型：`KnowledgePoint`、`KnowledgeEdge`。
- 建立资源模型：`Exercise`、`GeneratedResource`。
- 建立导入任务模型：`IngestJob`、`IngestJobItem`。
- 建立旧数据映射模型：`LegacyDataMapping`。
- 使用向量库负责语义检索和引用资料。
- 使用知识图谱负责知识点关系、先修关系、路径规划、掌握度分析。
- 第一版知识图谱可先用关系型表实现，后续再接 Neo4j。

## 6. `kg_rag_demo` 复用要求

- 必须复用原有数据，不重新构建数据集。
- 直接兼容 `kg_rag_demo/data/data/single_chunks.json`。
- 直接兼容 `kg_rag_demo/data/data/multi_chunks.json`。
- 直接兼容 `kg_rag_demo/data/data/qa_pairs.json`。
- 直接兼容 `kg_rag_demo/data/data/subgraphs.json`。
- 直接兼容 `kg_rag_demo/data/data/test_dataset.json`。
- 能复用已有 Chroma 向量数据就复用。
- 能复用已有 Neo4j 图谱数据就复用。
- 旧 memory 不复用、不迁移。
- 保留只读兼容模式：旧数据只读，新数据写入新系统。

## 7. 数据导入 Pipeline

- 做文件上传。
- 保存文件和元数据。
- 解析 PDF/PPT/Word/Markdown/TXT/代码文件。
- 清洗文本。
- 切分文档。
- 写入 `CourseDocument` 和 `DocumentChunk`。
- 生成 embedding。
- 写入向量库。
- 抽取知识点和关系。
- 写入轻量知识图谱。
- 记录导入进度。
- 支持断点续传。
- 支持增量导入。
- 支持失败文件和失败 chunk 记录。
- 支持旧 JSON 数据直接导入和字段映射。

## 8. 检索 Agent Pipeline

- 把 `kg_rag_demo` 的查询逻辑改成统一检索 Agent。
- 删除多余实验性检索方法。
- 删除过多 runtime config 开关。
- 默认不启用复杂 query rewrite。
- 默认不启用重型 reranker。
- 默认图谱 1 跳，必要时 2 跳。
- 默认 TopK 较小，保证速度。

检索流程：

```text
query + course_id + 当前阅读位置
  -> Query Normalize
  -> Scope Filter
  -> Vector Search + Keyword Search + KG Search
  -> Merge + Deduplicate
  -> 轻量 Rerank
  -> Context Pack
  -> RetrievalResult
```

检索 Agent 输出必须包含：

- query
- course_id
- document/page scope
- chunks
- kg_context
- citations
- score
- source_type
- knowledge_points

## 9. 多 Agent 架构

- 主 Agent 使用 ReAct。
- 主 Agent 负责对话、判断是否需要干活、读取记忆、调用 Planner、汇总结果。
- 闲聊或信息足够时，主 Agent 直接回复。
- 需要检索、资源生成、路径规划、信息不足时，主 Agent 调用 Planner。
- Planner 负责任务拆解、并行/串行规划、专家 Agent 输入输出定义。
- 专家 Agent 默认无长期记忆，只使用本次任务上下文。
- Review/Safety Agent 负责事实、引用、幻觉、敏感内容和格式审校。

专家 Agent 保留：

- 检索 Agent：资料检索、知识图谱查询、混合检索、上下文打包。
- 资源生成 Agent 组：练习题、笔记、思维导图、多模态讲解视频、实操案例、后续 PPT/动画/项目任务。
- 路径规划 Agent。

不单独设置：

- 画像分析 Agent：由主 Agent 维护 `user_profile`。
- 普通讲解 Agent：由主 Agent 基于检索结果讲解。
- 学习评估 Agent：由主 Agent 根据近期对话、阅读行为、练习表现更新 `user_profile`。

## 10. 资源生成

- 资源生成 Agent 组按资源类型拆分。
- Planner 可根据对话自动调用资源生成 Agent。
- 资源库页也允许用户手动选择类型强制生成。
- 用户可选择课程、知识点、难度、资源类型。
- 支持生成练习题。
- 支持生成笔记。
- 支持生成思维导图。
- 支持生成多模态讲解视频脚本。
- 支持生成实操案例。
- 生成结果保存到资源库。

## 11. 主 Agent 记忆

只保留 5 类：

- `me`：Agent 人设、回复方式、边界、语气。
- `user_profile`：用户画像和学习状态。
- `working_memory`：当前任务现场。
- `conversation_memory`：每天一个 JSON 对话记录。
- `memory`：重要长期记忆。

`user_profile` 必须记录：

- 专业、课程、学习目标。
- 知识基础。
- 近期正在学什么。
- 薄弱知识点。
- 经常错误的题型或概念。
- 知识点掌握度。
- 阅读行为。
- 练习表现。
- 资源偏好。
- 讲解偏好。

`conversation_memory`：

- 每天一个 JSON。
- 保存当天原始对话、摘要、课程、知识点、未解决问题、候选长期记忆。

旧 `kg_rag_demo` memory：

- 不读取。
- 不迁移。
- 不进入新 `user_profile`。

## 12. 记忆检索与压缩

- 每轮对话后更新当天摘要。
- 每天生成 `daily_summary`。
- 多天摘要合并为阶段总结。
- 相似 `memory` 合并。
- 过期、低置信度、被新证据推翻的记忆降权或归档。
- 每次回复只携带相关记忆。

每次上下文注入顺序：

```text
1. me
2. user_profile
3. working_memory
4. recent_conversation
5. retrieved_conversation
6. retrieved_memory
7. course_context
8. user_message
```

## 13. 后端实现任务

- 重做数据模型。
- 实现资料上传接口。
- 实现旧数据兼容读取。
- 实现数据导入任务。
- 实现文档解析和切分。
- 实现向量库适配。
- 实现知识图谱适配。
- 实现检索 Agent。
- 实现主 Agent。
- 实现 Planner。
- 实现资源生成 Agent 组。
- 实现路径规划 Agent。
- 实现 Review/Safety Agent。
- 实现 Memory Service。
- 实现资源库保存和查询。
- 实现阅读记录、练习记录、掌握度更新。

## 14. 前端实现任务

- 重做多页面路由。
- 做课程入口页。
- 做课程资料页。
- 做资料阅读页。
- 做多 Agent 学习页。
- 做学习路径页。
- 做练习与测评页。
- 做资源库页。
- 做智能体工作台页。
- 接入 Markdown/Mermaid/公式/代码渲染。
- 接入 PDF/PPT/Word 阅读能力。
- 接入文件上传和导入进度展示。
- 接入 Agent 流式输出。
- 接入专家 Agent 运行状态展示。

## 15. 验收任务

- 用原有 `test_dataset.json` 做检索回归测试。
- 验证旧 chunks 能检索。
- 验证旧 subgraphs 能查询。
- 验证旧 qa_pairs 能用于评估。
- 验证旧数据不需要重新构建。
- 验证旧 memory 不进入新系统。
- 验证资料阅读页能打开 PDF/PPT/Word。
- 验证 Markdown/Mermaid/代码/公式渲染正常。
- 验证主 Agent 能判断是否调用 Planner。
- 验证 Planner 能并行调用专家 Agent。
- 验证资源页能手动选择资源类型生成。

## 16. 主 Agent Tool 与 Skill 机制

### 16.1 Tool Registry

- 建立主 Agent 的 `ToolRegistry`。
- 所有工具通过注册表暴露给主 Agent。
- 主 Agent 不直接依赖具体实现，只通过工具名、描述、参数 schema 调用。
- Tool 需要支持启用/禁用、权限控制、超时、日志、错误返回。

### 16.2 主 Agent 基础 Tool List

主 Agent 至少需要以下工具：

- `web_search`：网页搜索，用于查询实时信息、开放资料和外部参考。
- `memory_search`：搜索 `memory`、`conversation_memory`、`user_profile`。
- `memory_write`：写入或更新 `user_profile`、`conversation_memory`、`memory`。
- `retrieval_search`：调用检索 Agent pipeline，查询课程资料、向量库和知识图谱。
- `course_context`：读取当前课程、资料、页码、选中文本和附近 chunk。
- `planner_call`：调用 Planner 生成任务计划。
- `agent_call`：按 Planner 结果调用专家 Agent。
- `resource_generate`：调用资源生成 Agent 组。
- `path_plan`：调用路径规划 Agent。
- `safety_review`：调用 Review/Safety Agent 审校输出。
- `file_read`：读取系统内允许访问的课程资料或生成资源。
- `user_profile_update`：根据近期对话、阅读行为、练习结果更新用户画像。

### 16.3 Tool 元数据

每个 Tool 注册时必须包含：

- `name`
- `description`
- `input_schema`
- `output_schema`
- `permission`
- `timeout`
- `side_effect`
- `examples`
- `enabled`

### 16.4 Skill 机制

- 设计 `SkillRegistry`。
- Skill 放到固定目录，例如 `skills/`。
- 每个 Skill 一个独立文件夹。
- 每个 Skill 至少包含 `SKILL.md` 和可选的 `tools.py`、`prompts/`、`examples/`。
- 主 Agent 可以按需加载 Skill。
- Skill 不默认全部塞进上下文。

建议目录：

```text
skills/
  exam_coach/
    SKILL.md
    examples/
  note_maker/
    SKILL.md
  mindmap_builder/
    SKILL.md
  code_lab_tutor/
    SKILL.md
  video_script_writer/
    SKILL.md
```

### 16.5 Skill 注册格式

`SKILL.md` 中至少声明：

- `name`
- `description`
- `when_to_use`
- `inputs`
- `outputs`
- `tools_required`
- `prompt_template`
- `examples`

注册流程：

```text
扫描 skills/
  -> 读取每个 SKILL.md 的 metadata
  -> 注册到 SkillRegistry
  -> 主 Agent 只看到 skill name、description、when_to_use
  -> 需要使用时再加载完整 SKILL.md
```

### 16.6 渐进式披露加载

Skill 必须采用渐进式披露：

1. 默认阶段
   - 主 Agent 只加载 Skill 索引。
   - 只包含名称、用途、触发条件。

2. 选择阶段
   - 主 Agent 判断当前任务可能需要某个 Skill。
   - 加载该 Skill 的完整 `SKILL.md`。

3. 执行阶段
   - 如果 Skill 需要专用 Tool，再加载对应工具定义。
   - 执行完成后只把结果摘要放回主 Agent 上下文。

4. 压缩阶段
   - Skill 执行过程不长期占用上下文。
   - 重要结果写入 `working_memory`、`user_profile` 或 `memory`。

### 16.7 初始 Skill List

- `exam_coach`：考试复习、错题分析、测评规划。
- `note_maker`：把资料或对话整理成结构化笔记。
- `mindmap_builder`：生成知识点思维导图。
- `exercise_designer`：生成分层练习题。
- `code_lab_tutor`：生成和辅导代码实操案例。
- `video_script_writer`：生成多模态讲解视频脚本。
- `resource_reviewer`：审查生成资源是否准确、完整、适合学生画像。

### 16.8 验收点

- 主 Agent 有可查看的 Tool List。
- Tool 可以注册、禁用、启用。
- 主 Agent 能调用网页搜索。
- 主 Agent 能调用记忆搜索。
- 主 Agent 能调用课程检索。
- Skill 能从 `skills/` 自动注册。
- Skill 默认只加载索引。
- 只有需要时才加载完整 Skill 内容。
- Skill 执行后能把结果返回主 Agent。

# 个性化学习多智能体系统重设计

## 1. 当前实现问题

当前版本更像“一键生成 Demo”，不是完整学习系统。主要问题：

- 没有课程资料上传与知识库构建流程。
- 前端把画像、资源、路径、评估都塞在一个页面里。
- 多 Agent 只是固定顺序调用，缺少 Planner 拆解和并行执行。
- 用户画像、学习行为、知识掌握和对话记忆没有形成长期闭环。
- RAG 只做了简单资料检索，没有知识图谱支撑路径规划和掌握度分析。

后续应以重设计为主，不继续在旧结构上堆功能。

## 2. 系统核心输入

系统有两类输入：

1. 课程资料输入
   - 包括 PDF、PPT、Word、Markdown、TXT、代码案例、题库等。
   - 上传后生成课程数据库、向量库和知识图谱。
   - 后续回答、讲解、路径、题目都必须基于课程资料。

2. 用户对话输入
   - 用户通过自然语言表达学习目标、问题、反馈和偏好。
   - 主 Agent 根据对话更新用户画像、调用多 Agent、生成学习支持。

## 3. 系统核心功能

系统围绕两个核心功能设计：

1. 用户可以阅读资料
   - 浏览课程、章节、课件、文档、代码案例和题库。
   - 支持搜索、高亮、笔记、收藏、阅读进度。
   - 阅读行为会进入用户画像和学习记录。

2. 用户可以和多 Agent 系统交互学习
   - 用户可围绕当前资料、知识点、题目或学习目标提问。
   - Agent 根据课程知识库和用户画像进行讲解、追问、出题、评估和路径调整。

两者必须联动：资料阅读提供知识依据，多 Agent 交互提供个性化辅导。

## 4. 页面结构

不要做成一个大页面，改为多页面应用：

- 首页/课程入口页：课程列表、最近学习、进度、推荐继续学习。
- 课程资料页：课程目录、章节、课件、文档、代码案例、题库入口。
- 资料阅读页：阅读单份资料，支持搜索、高亮、笔记、收藏和进度。
- 多 Agent 学习页：围绕当前课程或资料进行对话、讲解、追问、诊断、资源生成。
- 学习路径页：阶段学习计划、推荐资源、完成状态、下一步任务。
- 练习与测评页：题库、练习记录、错题分析、掌握度变化。
- 资源库页：系统生成的讲解文档、思维导图、练习题、代码案例、视频脚本等。
- 智能体工作台页：展示 Planner 任务拆解、专家 Agent 状态、审校结果和重试记录。

## 4.1 前端资料阅读与内容渲染能力

前端必须具备两类基础能力：

1. 正确渲染 Agent 输出和生成资源。
2. 正确阅读用户上传的课程资料。

### Markdown 渲染

多 Agent 回复、生成笔记、讲解文档、题目解析、学习路径说明等内容应统一使用 Markdown 渲染。

需要支持：

- 标题、段落、列表、引用。
- 表格。
- 代码块和语法高亮。
- 数学公式。
- 图片。
- 链接。
- Mermaid 图，例如思维导图、流程图、知识结构图。

### 资料阅读

系统应支持阅读常见课程资料格式：

- PDF。
- PPT/PPTX。
- Word/DOCX。
- Markdown。
- TXT。
- 代码文件。

资料阅读页需要做到：

- 显示原始文件内容或转换后的可阅读内容。
- 支持章节/页码导航。
- 支持文本搜索。
- 支持选中文本后向多 Agent 提问。
- 支持高亮、笔记、收藏。
- 支持记录阅读进度。
- 支持把当前阅读位置作为 `working_memory` 传给主 Agent。

### 文件处理方式

前端负责阅读体验，后端负责文件解析和内容提取。

推荐处理方式：

- PDF：前端使用 PDF viewer 展示，后端提取文本用于检索。
- PPT/PPTX：后端转换为图片或 HTML/文本结构，前端按页展示。
- Word/DOCX：后端解析为 HTML/Markdown，前端阅读。
- Markdown/TXT：直接渲染。
- 代码文件：使用代码高亮组件展示。

### 与 Agent 的联动

资料阅读器必须能把当前上下文传给主 Agent：

- `course_id`
- `document_id`
- `page`
- `section`
- `selected_text`
- `nearby_chunks`
- `user_note`

这样用户在资料里选中一段内容问“这里看不懂”时，主 Agent 能基于当前位置、课程知识库和用户画像进行回答。

## 5. 知识库设计

采用“向量库 + 知识图谱”的双层知识底座。

### 向量库

负责找资料、引用资料、支撑 RAG 问答。

存储对象：

- `DocumentChunk`
- 原文内容
- 来源文件
- 页码/章节
- 知识点标签
- embedding

### 知识图谱

负责组织知识、规划路径、解释推荐、追踪掌握度。

核心节点：

- `Course`
- `Chapter`
- `KnowledgePoint`
- `Document`
- `Chunk`
- `Exercise`
- `Resource`

核心关系：

- `contains`
- `prerequisite_of`
- `related_to`
- `explains`
- `tests`
- `generated_from`

### 落地顺序

1. 先实现资料上传、解析、切分、向量检索、引用。
2. 再用关系型数据库实现轻量知识图谱。
3. 后续复杂度上来后再考虑 Neo4j 等图数据库。

## 6. 多 Agent 架构

采用：

```text
主 Agent + Planner Agent + 专家 Agent + Review/Safety Agent
```

流程：

```text
用户输入
  ↓
主 Agent 理解意图并读取记忆
  ↓
Planner Agent 拆解任务
  ↓
并行调用专家 Agent
  ↓
Review/Safety Agent 审校
  ↓
主 Agent 汇总回复用户
```

### 主 Agent

- 直接和用户对话。
- 管理用户记忆。
- 调用 Planner。
- 汇总专家 Agent 输出。
- 形成最终回复。

### Planner Agent

- 根据用户输入、当前页面、课程资料、用户画像拆解任务。
- 判断哪些任务可并行，哪些任务必须串行。
- 输出结构化任务计划。

### 专家 Agent

专家 Agent 默认无长期记忆，只使用本次任务上下文。

建议专家：

- 检索 Agent：统一负责资料检索、知识图谱查询、混合检索和上下文打包。
- 资源生成 Agent 组：按资源类型拆分为练习题 Agent、多模态讲解视频 Agent、笔记 Agent、思维导图 Agent、实操案例 Agent 等。
- 路径规划 Agent

不再单独设置画像分析 Agent、讲解 Agent、学习评估 Agent：

- 用户画像由主 Agent 维护在 `user_profile` 中。
- 普通讲解由主 Agent 基于检索结果和用户画像完成。
- 学习评估也写入 `user_profile`，由主 Agent 根据近期对话、阅读行为、练习表现和常错点持续更新。

### Review/Safety Agent

- 检查输出是否基于课程资料。
- 检查事实错误、幻觉、敏感内容和格式问题。
- 必要时要求某个专家 Agent 重跑。

## 7. Agent 记忆设计

记忆只属于主 Agent 和 Memory Service。专家 Agent 不保存长期记忆。

保留 5 类记忆：

### `me`

Agent 自身设定。

包含：

- 角色
- 回复方式
- 语气
- 边界
- 是否主动追问
- 是否简洁

### `user_profile`

用户画像，也是主 Agent 维护学习状态和近期评估结果的核心位置。

包含：

- 专业、课程、学习目标
- 知识基础、薄弱知识点
- 用户近期正在学习的内容
- 经常错误的知识点、题型或概念
- 知识点掌握度
- 阅读行为
- 练习表现
- 资源偏好
- 讲解偏好

重要字段包含：

- `value`
- `confidence`
- `evidence`
- `updated_at`

### `working_memory`

当前任务临时上下文。

包含：

- 当前页面
- 当前课程
- 当前资料
- 选中文本
- 用户本轮问题
- Planner 任务计划
- 专家 Agent 输出草稿

任务结束后不直接长期保存，由 Memory Writer 判断是否写入其他记忆。

### `conversation_memory`

每天一个 JSON 文件，以日期命名。

例如：

```text
conversations/2026-06-07.json
```

保存：

- 当天原始对话
- 当天摘要
- 涉及课程
- 涉及知识点
- 未解决问题
- 可写入长期记忆的候选项

### `memory`

重要长期记忆。

记录：

- 用户明确要求记住的事
- 重要学习事件
- 踩过的坑
- 长期稳定偏好
- 关键约定

例如：

- 用户要求以后解释概念时先给例子。
- 用户在反向传播中连续多次卡在链式法则。
- 用户之前踩过环境变量配置的坑。

## 8. 记忆压缩机制

记忆不能无限进入上下文。

压缩规则：

- 每轮对话后更新当天 `summary`。
- 每天生成 `daily_summary`。
- 每周或每阶段合并多个 `daily_summary`，更新 `user_profile`。
- `memory` 中相似记忆合并。
- 过期、低置信度、被新证据推翻的记忆降权或归档。

## 9. 记忆搜索机制

主 Agent 每次回复前只搜索相关记忆。

搜索来源：

- `me`：默认读取精简版本。
- `user_profile`：结构化字段检索。
- `conversation_memory`：按日期、课程、知识点、关键词、摘要检索。
- `memory`：按语义相似度、标签、时间权重检索。

优先级：

1. 当前任务相关。
2. 用户明确要求记住的内容。
3. 最近发生的内容。
4. 高置信度内容。

## 10. 每次回复携带的记忆预算

默认只携带少量相关记忆：

- `me`：300-500 字。
- `user_profile`：500-1000 字，只取相关字段。
- `working_memory`：1000-3000 字，包含当前资料片段和任务上下文。
- `conversation_memory`：最近 3-5 轮摘要，必要时检索历史日期摘要。
- `memory`：Top 3-5 条最相关重要记忆。

上下文注入顺序：

```text
1. me
2. user_profile
3. working_memory
4. retrieved_conversation
5. retrieved_memory
6. course_context
7. user_message
```

## 11. 初步数据模型

课程与资料：

- `Course`
- `CourseDocument`
- `DocumentChunk`
- `KnowledgePoint`
- `KnowledgeEdge`
- `Exercise`
- `GeneratedResource`

用户与学习：

- `User`
- `UserProfile`
- `ReadingRecord`
- `LearningActivity`
- `LearningPath`
- `MasteryRecord`

Agent 与记忆：

- `Conversation`
- `DailyConversationMemory`
- `MemoryItem`
- `AgentRun`
- `AgentTask`

## 12. 下一步实现顺序

1. 重做数据库模型。
2. 做课程资料上传与解析。
3. 做文档切分、向量检索和引用。
4. 做轻量知识图谱。
5. 重做多页面前端。
6. 实现主 Agent、Planner、专家 Agent 并行编排。
7. 实现 Memory Service。
8. 接入资源生成、路径规划、练习测评和智能体工作台。

## 13. 专家 Agent 与工具调用方式

不是每个专家 Agent 都需要采用 ReAct 架构。

## 14. 总体 Agent 运行流程

确认采用以下总流程：

```text
用户输入
  ↓
主 Agent（ReAct）
  ↓
判断是否需要开始执行任务
  ├─ 闲聊/普通对话/信息足够：主 Agent 直接回复
  └─ 需要资料、生成、规划、检索或信息不足：调用 Planner
        ↓
      Planner 拆解目标
        ↓
      调用对应专家 Agent
        ↓
      Review/Safety 审校
        ↓
      专家结果返回主 Agent
        ↓
      主 Agent 汇总结果并继续和用户对话
```

### 主 Agent 什么时候不调用 Planner

- 用户只是在闲聊。
- 用户问的是主 Agent 已经能直接回答的简单问题。
- 当前问题不需要课程资料、资源生成、路径规划或复杂工具。
- 主 Agent 已经掌握足够上下文，可以直接解释。

### 主 Agent 什么时候调用 Planner

- 需要检索课程资料或知识图谱。
- 需要生成个性化资源。
- 需要规划学习路径。
- 需要调用多个专家 Agent 协作。
- 当前信息不足，需要先收集资料、画像、上下文或历史记录。
- 用户明确要求“生成”“整理”“规划”“出题”“做思维导图”“做视频脚本”等。

### Planner 的职责

Planner 不直接和用户聊天，负责把主 Agent 的目标拆成结构化任务：

- 要调用哪些专家 Agent。
- 哪些任务可以并行。
- 哪些任务必须依赖前置结果。
- 每个专家 Agent 的输入是什么。
- 预期输出格式是什么。

### 专家结果如何回到主 Agent

所有运行过的专家 Agent 都返回结构化结果，包括：

- `agent_name`
- `task_id`
- `status`
- `input_summary`
- `output`
- `sources`
- `confidence`
- `errors`
- `cost_time`

主 Agent 拿到这些结果后，不是简单拼接，而是继续以对话方式组织输出：

- 总结专家结果。
- 根据用户当前问题选择重点。
- 继续追问缺失信息。
- 更新 `user_profile` 或 `memory`。
- 决定是否需要下一轮 Planner。

### 结论

主 Agent 是 ReAct 式总控和对话入口；Planner 是任务规划器；专家 Agent 是可并行执行的能力模块；最终解释、追问、对话连续性和记忆更新都回到主 Agent。

### 基本原则

- 主 Agent 和 Planner 可以具备较强推理能力。
- 专家 Agent 应尽量职责单一、输入输出稳定。
- 检索、解析、入库、权限过滤、引用生成这类确定性任务，不应交给 Agent 自由发挥。
- ReAct 适合多步推理和需要选择工具的任务，不适合所有模块。

### 数据检索设计

数据检索部分采用固定 pipeline，而不是让专家 Agent 自行随意调用底层工具。

推荐检索 pipeline：

```text
用户问题/当前资料位置
  ↓
Query Rewrite：改写检索 query
  ↓
Scope Filter：限定课程、章节、资料、权限
  ↓
Hybrid Search：向量检索 + 关键词检索 + 知识图谱查询
  ↓
Rerank：重排序
  ↓
Context Pack：组装引用片段、来源、页码、知识点
  ↓
返回给 Agent
```

### 为什么检索要固定 pipeline

- 结果稳定，方便测试和调试。
- 能统一处理权限、课程范围和资料来源。
- 能保证回答有引用依据。
- 能避免 Agent 漏掉重排序、过滤、引用等关键步骤。
- 便于后续替换向量库、图数据库或 reranker。

### Agent 如何使用检索

检索系统对外暴露为一个高层工具或专家服务：

```text
RetrievalTool.search(query, course_id, document_id, user_context)
```

Agent 可以决定“是否需要检索”和“检索什么”，但不能绕过检索 pipeline 直接操作底层向量库或图谱。

### 哪些 Agent 适合 ReAct

适合：

- 主 Agent：判断意图、决定是否查资料、是否调用 Planner。
- Planner Agent：拆任务、决定并行和串行关系。
- 复杂辅导 Agent：在讲解、追问、查资料、出题之间多步切换。

不太适合：

- 检索 Agent：内部应走固定 pipeline，统一封装资料检索、知识图谱查询、混合检索和上下文打包。
- 文档解析 Agent：应走固定解析流程。
- 向量入库 Agent：应走固定入库流程。
- Safety Agent：应按规则和模型审校流程执行。
- 学习评估：不单独作为专家 Agent，评估结论写入 `user_profile`，由主 Agent 根据近期学习证据维护。

### 结论

采用混合方式：

- 检索和数据处理：固定 pipeline。
- 主 Agent/Planner/复杂学习辅导：可使用 ReAct。
- 专家 Agent：默认无状态、接口稳定，只在确实需要多步工具选择时使用 ReAct。

# 系统改进讨论记录

## 2026-06-07 初始记录

### 当前判断

用户指出：当前系统的实现方式和整体设计存在问题，需要重新设计应用。后续讨论以“先重新定义产品和架构，再决定是否重写代码”为主，不继续在现有实现上盲目增加功能。

### 当前实现的主要问题

1. 产品形态偏“演示页”，不像一个真正的学习智能体系统。
   - 当前前端一次输入后直接生成所有内容，缺少持续学习过程。
   - 学生画像、学习路径、资源生成、评估之间还没有形成真实闭环。

2. 多智能体更像顺序调用模块，不像真正的协作系统。
   - 当前只是固定执行 `ProfileAgent -> CourseAnalysisAgent -> ResourceAgent -> SafetyAgent -> PlannerAgent -> EvaluatorAgent -> TutorAgent`。
   - 缺少任务分解、智能体间协商、反思、重试、审核和状态持久化。

3. 学习画像过于静态。
   - 当前画像由启发式规则生成，虽然满足演示，但不够可信。
   - 画像应来自多轮对话、诊断测验、学习行为、错题记录和资源反馈。

4. 资源生成缺少真实工作流。
   - 当前资源一次性生成，缺少“需求分析 -> 草稿 -> 审核 -> 修改 -> 发布”的资源生产过程。
   - 视频、动画、PPT、题库等多模态资源目前只是文本脚本级别，展示力不足。

5. 课程知识库与 RAG 较弱。
   - 当前是本地 JSON + 关键词检索，不够像真实知识库系统。
   - 应支持课程资料上传、切分、索引、引用、版本管理和知识点图谱。

6. 评估系统不够真实。
   - 当前评估是规则生成，没有完整的练习提交、错因分析、掌握度更新和路径调整。

7. 工程边界需要重新划分。
   - 应区分学生端、教师/管理员端、智能体工作台、资源库、学习记录等模块。
   - 当前代码虽然有接口，但业务域模型仍然偏简单。

### 需要重新讨论的核心方向

1. 系统到底面向谁？
   - 只面向学生个人学习，还是同时面向教师构建课程资源？

2. 系统核心体验是什么？
   - 是“聊天式 AI 学习助手”，还是“个性化学习平台”，还是“资源生成工作台 + 学生学习端”？

3. 多智能体应该如何体现？
   - 是后台自动协作，还是前端可视化展示每个智能体的任务、输入、输出和审核过程？

4. 多模态资源做到什么程度？
   - 文本 + Markdown + Mermaid 是否足够，还是要真正生成 PPT、图片、音频或视频脚本文件？

5. 是否要推翻现有代码？
   - 可以保留 FastAPI + Next.js 技术栈，但重做产品结构、数据模型和交互流程。

### 初步重设计倾向

建议将系统从“一键生成页面”改为“三端一体”的学习智能体平台：

- 学生端：对话画像、诊断测验、学习路径、资源学习、练习反馈、智能辅导。
- 教师/课程端：课程知识库管理、资源模板管理、知识点图谱、生成任务审核。
- 智能体工作台：展示多智能体任务流，包括规划、检索、生成、审校、评估和反思。

后续根据用户反馈继续调整。

## 2026-06-07 输入体系确认

### 用户观点

系统的输入必须先定义清楚，至少包含两类：

1. 课程课件和资料输入。
   - 用于生成课程数据库、知识库、向量索引、知识点结构和后续 RAG 检索依据。
   - 系统必须提供“上传数据并生成数据库/知识库”的功能。

2. 用户对话输入。
   - 学生通过自然语言表达专业背景、学习目标、当前困难、兴趣偏好、学习反馈等。
   - 系统根据对话持续更新学生画像，并驱动个性化资源生成、路径规划和智能辅导。

### 结论

认同。这个判断应作为新架构的基础原则：系统不是直接让用户输入一句话然后生成资源，而是先由课程资料构建可靠的课程知识底座，再由学生对话提供个性化上下文。两类输入分别对应两个核心子系统：

- 课程知识库构建系统：上传、解析、切分、清洗、入库、索引、知识点抽取、版本管理。
- 个性化学习交互系统：对话、画像、诊断、检索、生成、评估、路径调整。

### 对架构的影响

1. 必须新增“课程资料管理/上传”模块。
   - 支持上传 PDF、PPT、Word、Markdown、TXT、代码文件等资料。
   - 上传后进入处理流水线，而不是直接丢给大模型。

2. 必须新增“知识库构建流水线”。
   - 文档解析。
   - 文本清洗。
   - 文档切分。
   - 元数据标注。
   - 向量化索引。
   - 关键词索引。
   - 知识点和章节结构抽取。
   - 入库状态追踪。

3. 学生端生成内容时必须依赖已构建的课程知识库。
   - 学生对话只提供个性化需求。
   - 课程事实、知识点和资源依据必须来自课程资料库。

4. 数据模型需要重做。
   - `Course`
   - `CourseDocument`
   - `DocumentChunk`
   - `KnowledgePoint`
   - `VectorIndex`
   - `StudentProfile`
   - `Conversation`
   - `GeneratedResource`
   - `LearningPath`
   - `LearningActivity`

### 新的产品结构倾向

系统至少应有两个主入口：

1. 课程建设入口。
   - 教师或管理员上传课程资料。
   - 系统生成课程数据库和知识库。
   - 展示资料处理状态、知识点结构和索引结果。

2. 学生学习入口。
   - 学生通过对话输入学习需求。
   - 系统从课程知识库中检索依据。
   - 多智能体基于学生画像生成资源、路径和辅导。

## 2026-06-07 核心功能收敛

### 用户观点

系统应围绕两个核心功能展开：

1. 用户可以阅读资料。
2. 用户可以和多 Agent 系统交互来学习。

### 结论

认同。这个定义比“一键生成资源”更接近真实学习产品。系统的核心不是单纯生成内容，而是围绕课程资料形成一个可学习、可提问、可反馈、可持续更新的学习环境。

### 产品结构

新的系统可以理解为两个相互连接的学习空间：

1. 资料阅读空间。
   - 用户进入某门课程后，可以浏览课程资料、章节、知识点、课件、文档、代码案例和题库。
   - 阅读过程应支持高亮、笔记、收藏、进度记录、资料内搜索。
   - 阅读资料不是静态展示，系统需要记录用户看过什么、停留在哪里、标记了哪些不懂的地方。

2. 多 Agent 交互学习空间。
   - 用户围绕正在阅读的资料、某个知识点、某道题或自己的学习目标与系统对话。
   - 多 Agent 系统基于课程知识库和用户画像进行讲解、提问、诊断、生成练习、规划路径和调整资源。
   - 对话不应脱离资料，而应能引用当前阅读位置、课程章节和知识库证据。

### 两个核心功能之间的关系

资料阅读空间提供“学习上下文”和“知识依据”，多 Agent 交互空间提供“个性化理解、辅导和生成”。二者不是并列孤岛，而应该互相驱动：

- 用户阅读资料时遇到不懂的内容，可以直接唤起多 Agent 解释。
- 多 Agent 推荐的资源和路径，可以跳转回具体资料、章节或知识点。
- 用户的阅读进度、笔记、错题和提问，会反向更新学习画像。
- 学习画像变化后，会影响后续资料推荐、讲解深度和练习难度。

### 对当前实现的改造影响

当前前端的一页式“输入后生成所有内容”需要改成多页面应用，不要把所有功能塞进一个页面。每个页面应围绕一个明确任务设计：

- 首页/课程入口页：展示课程列表、最近学习、学习进度和推荐继续学习内容。
- 课程资料页：展示课程目录、章节、课件、文档、代码案例和题库入口。
- 资料阅读页：专注阅读单份资料，支持目录、搜索、高亮、笔记、收藏和阅读进度。
- 多 Agent 学习页：围绕当前课程或当前资料进行对话、讲解、追问、诊断和资源生成。
- 学习路径页：展示阶段性学习计划、推荐资源、完成状态和下一步任务。
- 练习与测评页：展示题库、练习记录、错题分析和掌握度变化。
- 资源生成/资源库页：展示系统生成的讲解文档、思维导图、题目、代码案例、视频脚本等资源。
- 智能体工作台页：可视化展示多 Agent 的任务分解、执行过程、审校结果和重试记录。

当前后端也需要从一次性编排改为围绕学习会话和资料上下文运行：

- 每次对话都应携带 `course_id`、`document_id`、`chunk_id` 或当前阅读位置。
- 多 Agent 的输入应包含学生画像、课程知识片段、阅读行为和对话历史。
- 系统需要持久化阅读记录、学习会话、生成资源和评估结果。

## 2026-06-07 知识库方案讨论

### 用户问题

课程资料上传后，数据库应该采用“向量库 + 知识图谱”的方式构建，还是只使用一个向量库更好？

### 判断

更推荐采用“向量库 + 知识图谱”的混合方案，而不是只用向量库。

### 原因

1. 向量库适合解决“从资料中找相关内容”的问题。
   - 用于语义检索课件、文档、教材片段、代码注释、题目解析等。
   - 适合回答“这段内容是什么意思”“帮我解释这个知识点”“查找相关资料”。
   - RAG 主要依赖向量库提供上下文证据。

2. 知识图谱适合解决“课程结构和学习关系”的问题。
   - 表达课程、章节、知识点、先修知识、后继知识、题目、资源之间的关系。
   - 适合学习路径规划、知识点掌握度更新、错题归因和资源推荐。
   - 能让系统知道“学神经网络前应先掌握函数、梯度、监督学习和损失函数”。

3. 只用向量库会有明显短板。
   - 向量库能找相似内容，但不擅长表达知识点依赖关系。
   - 难以稳定生成学习路径。
   - 难以解释为什么推荐某个章节或题目。
   - 难以做知识掌握度追踪。

4. 只用知识图谱也不够。
   - 图谱表达结构强，但不适合直接承载大段课件原文。
   - 对开放式问答、资料引用、长文档解释不如向量检索方便。

### 推荐架构

课程资料上传后，应同时生成两类知识资产：

1. 向量知识库。
   - 存储文档切片 `DocumentChunk`。
   - 每个切片包含文本内容、来源文件、页码/章节、知识点标签、向量 embedding。
   - 用于 RAG 检索和资料阅读时的上下文定位。

2. 课程知识图谱。
   - 节点包括 `Course`、`Chapter`、`KnowledgePoint`、`Document`、`Chunk`、`Exercise`、`Resource`。
   - 边包括 `contains`、`prerequisite_of`、`related_to`、`explains`、`tests`、`generated_from`。
   - 用于路径规划、资源推荐、错因分析和学习进度可视化。

### 工程落地建议

初期可以分阶段实现：

1. 第一阶段：先实现向量库。
   - 确保资料上传、解析、切分、检索、引用可跑通。
   - 这是 RAG 和问答的基础。

2. 第二阶段：实现轻量知识图谱。
   - 不必一开始上复杂图数据库。
   - 可以先用关系型数据库表表达节点和边，例如 `knowledge_points`、`knowledge_edges`。
   - 前端可以展示知识点关系和学习路径。

3. 第三阶段：再考虑专业图数据库。
   - 如果后续图谱复杂度上升，再接 Neo4j 或其他图数据库。
   - 比赛初版不一定必须引入重型图数据库，避免工程复杂度过高。

### 结论

系统设计上应明确采用“向量库 + 知识图谱”的双层知识底座：

- 向量库负责“找资料、引用资料、支撑问答”。
- 知识图谱负责“组织知识、规划路径、解释推荐、追踪掌握度”。

这样比单纯向量库更符合个性化学习系统，也更能体现赛题要求中的智能化、个性化和多智能体协同。

## 2026-06-07 多 Agent 架构选择

### 用户观点

多 Agent 系统可以采用 `Planner + 专家 Agent` 架构。Planner 负责拆解任务，专家 Agent 负责执行具体子任务。多个专家 Agent 可以并行运行，从而提高系统响应速度。

### 结论

认同。新的多 Agent 架构采用“主 Agent + Planner + 专家 Agent + 审校 Agent”的模式，而不是简单固定流水线。

整体流程：

```text
用户输入
  ↓
主 Agent
  ↓
Planner Agent 拆解任务
  ↓
并行调用多个专家 Agent
  ├─ 检索 Agent
  ├─ 资源生成 Agent 组
  │   ├─ 练习题 Agent
  │   ├─ 多模态讲解视频 Agent
  │   ├─ 笔记 Agent
  │   ├─ 思维导图 Agent
  │   └─ 实操案例 Agent
  └─ 路径规划 Agent
  ↓
Review / Safety Agent 审校
  ↓
主 Agent 汇总回复用户
```

### 角色设计

1. 主 Agent。
   - 负责和用户直接对话。
   - 判断用户意图。
   - 调用 Planner。
   - 汇总专家 Agent 输出。
   - 形成最终回答或生成结果。

2. Planner Agent。
   - 根据用户输入、当前页面上下文、课程资料上下文和学生画像拆解任务。
   - 输出结构化任务计划。
   - 标记哪些任务可以并行，哪些任务必须串行。

3. 专家 Agent。
   - 每个专家只负责一个清晰能力。
   - 输入输出格式固定，便于并行、测试和替换。
   - 检索 Agent 统一负责资料检索、知识图谱查询、混合检索和上下文打包。
   - 资源生成 Agent 组按资源类型拆分，例如练习题、多模态讲解视频、笔记、思维导图、实操案例。
   - 不再单独设置画像分析、普通讲解、学习评估专家；这些由主 Agent 结合 `user_profile` 完成。

4. Review / Safety Agent。
   - 检查专家输出是否基于课程资料。
   - 检查是否存在事实错误、敏感内容、幻觉和格式问题。
   - 必要时要求某个专家 Agent 重新执行。

### 资源生成 Agent 组

资源生成不是一个单独的大 Agent，而是一组按资源类型拆分的专家 Agent：

- 练习题 Agent
- 多模态讲解视频 Agent
- 笔记 Agent
- 思维导图 Agent
- 实操案例 Agent
- 后续可扩展 PPT Agent、动画分镜 Agent、项目任务 Agent

资源生成有两种触发方式：

1. Planner 自动触发。
   - 用户在学习对话中提出“给我几道题”“帮我整理笔记”“画个思维导图”等需求。
   - Planner 根据意图调用对应资源生成 Agent。

2. 资源页面手动触发。
   - 用户进入资源库页，选择课程、知识点、难度和资源类别。
   - 用户勾选要生成的类型，例如练习题、视频脚本、笔记、思维导图、实操案例。
   - 系统按选择调用对应资源生成 Agent，并把结果保存到资源库。

### 并行执行原则

Planner 需要区分任务依赖关系：

- 可以并行的任务：
  - 检索 Agent 内部并行执行向量检索、关键词检索和知识图谱查询。
  - 主 Agent 读取 `user_profile`。
  - 对话历史摘要。

- 需要等待检索结果后执行的任务：
  - 主 Agent 生成普通讲解。
  - 资源生成 Agent 组生成题目、笔记、思维导图、视频脚本、实操案例等。
  - 智能辅导回答。

- 需要等待学习行为、近期对话或答题结果后执行的任务：
  - 主 Agent 更新 `user_profile` 中的掌握度、薄弱点和常错点。
  - 学习路径调整。
  - 个性化资源推荐。

### 示例

当用户在资料阅读页选中一段内容并提问“这段看不懂，能不能讲简单点并给我两道题？”时：

1. 主 Agent 接收问题和当前阅读位置。
2. Planner 拆成多个任务：
   - 检索当前资料上下文。
   - 读取 `user_profile` 中的近期学习内容、薄弱点和常错点。
   - 主 Agent 生成通俗讲解。
   - 调用练习题 Agent 生成两道练习题。
   - 审校输出。
3. 检索 Agent 内部可以并行执行向量检索、关键词检索和知识图谱查询。
4. 主 Agent 讲解和练习题 Agent 在拿到上下文后可以并行。
5. Review Agent 统一审校。
6. 主 Agent 汇总成面向学生的回答。

### 前端展示方式

多 Agent 学习页或智能体工作台页可以展示：

- Planner 生成的任务列表。
- 每个专家 Agent 的运行状态：等待、运行中、完成、失败、重试。
- 每个 Agent 的输入摘要、输出摘要和耗时。
- 哪些任务是并行执行的。
- Review Agent 的审校结论。

这样既能提升系统可解释性，也能在比赛演示中清楚体现“多智能体协同”。

## 2026-06-07 Agent 记忆边界

### 用户问题

系统中应该有一个主要 Agent 负责和用户对话，记忆也属于这个主 Agent。那么专家 Agent 是否也需要记忆？

### 判断

应该有一个主对话 Agent 负责用户交互和长期记忆。专家 Agent 默认不保留长期记忆，只在单次任务内使用临时上下文。

### 设计结论

1. 主对话 Agent 拥有用户级长期记忆。
   - 负责保存学生画像。
   - 负责维护对话历史摘要。
   - 负责记录用户偏好、薄弱点、学习目标和学习习惯。
   - 负责将必要记忆注入 Planner 和专家 Agent 的任务上下文。

2. 专家 Agent 默认无长期记忆。
   - 专家 Agent 不直接记住用户历史。
   - 专家 Agent 每次只根据任务输入、课程资料、知识图谱结果和主 Agent 提供的上下文工作。
   - 这样更容易并行执行、测试、替换和复现。

3. 专家 Agent 可以有任务内短期记忆。
   - 例如一次资源生成任务中，资源生成 Agent 组可以记录草稿、修改意见、引用来源。
   - 练习题 Agent 可以记录本轮题目难度分布和覆盖知识点。
   - 这些记忆只属于当前任务，任务结束后作为结果返回给主 Agent 或写入系统日志。

4. 需要持久化的内容不直接存在专家 Agent 内部。
   - 画像更新写入 `StudentProfile`。
   - 学习行为写入 `LearningActivity`。
   - 对话摘要写入 `ConversationMemory`。
   - 资源生成记录写入 `GeneratedResource`。
   - Agent 执行过程写入 `AgentRunLog`。

### 为什么专家 Agent 不建议有长期记忆

1. 防止状态混乱。
   - 如果每个专家都有自己的记忆，很容易出现资源生成 Agent、路径规划 Agent 和主 Agent 对同一个学生理解不一致。

2. 方便并行。
   - 无长期记忆的专家 Agent 更像无状态服务，可以并行调度，也可以失败重试。

3. 方便替换。
   - 后续替换某个专家 Agent，不会丢失用户长期记忆。

4. 方便审计。
   - 所有长期记忆集中在主 Agent/记忆服务中，便于查看和修改。

### 推荐记忆架构

```text
用户
  ↓
主对话 Agent
  ↓ 读取/更新
Memory Service
  ├─ StudentProfile
  ├─ ConversationSummary
  ├─ LearningActivity
  ├─ WeaknessHistory
  └─ PreferenceHistory
  ↓ 注入上下文
Planner Agent
  ↓
专家 Agent（默认无长期记忆，只使用任务上下文）
```

### 例外情况

如果某个专家 Agent 确实需要积累经验，也不建议把记忆存在 Agent 对象内部，而应通过独立服务实现：

- 题目生成 Agent 可以读取 `QuestionGenerationHistory`，避免重复出题。
- 资源生成 Agent 可以读取 `ResourceGenerationHistory`，避免重复生成相似资源。
- 评估 Agent 可以读取 `AssessmentHistory`，观察掌握度变化。

这些本质上是业务数据或历史记录，不是专家 Agent 私有记忆。

## 2026-06-07 主 Agent 记忆设计

### 用户问题

主 Agent 的记忆应该如何设计？是否可以借鉴 Hermes？

### 判断

可以借鉴 Hermes 一类记忆系统的思想：把记忆从简单聊天记录升级为结构化、可检索、可更新、可遗忘的长期记忆系统。但不建议让主 Agent 自己把所有记忆都塞进上下文，而应设计独立的 `Memory Service`，由主 Agent 调用。

### 核心原则

1. 主 Agent 是记忆的使用者和维护者，不是唯一存储容器。
   - 主 Agent 负责判断什么时候读取记忆、写入记忆、更新画像和遗忘无用信息。
   - 实际记忆应存储在数据库、向量库和结构化画像表中。

2. 记忆必须分层。
   - 不同类型记忆的用途、生命周期和检索方式不同。
   - 不能只保存完整聊天记录，也不能只保存学生画像。

3. 记忆需要可解释。
   - 系统应该能说明“为什么认为学生薄弱点是监督学习”。
   - 每条重要记忆都应有来源，例如来自对话、测验、阅读行为或错题。

4. 记忆需要可更新和可遗忘。
   - 学生能力会变化，旧记忆不能永久支配推荐。
   - 掌握度、薄弱点、兴趣偏好都应有时间权重和置信度。

### 推荐记忆分层

1. 工作记忆 `WorkingMemory`。
   - 当前对话窗口内的短期上下文。
   - 包括当前问题、当前阅读资料、选中文本、当前任务计划、专家 Agent 输出。
   - 生命周期：一次会话或一次任务。

2. 对话记忆 `ConversationMemory`。
   - 多轮对话摘要，不保存所有原文到提示词中。
   - 记录用户最近讨论过的问题、未解决困惑、主 Agent 已给出的建议。
   - 用于保持聊天连续性。

3. 学生画像记忆 `StudentProfileMemory`。
   - 结构化保存学生专业、基础、目标、偏好、薄弱点、认知风格、时间投入、实践能力等。
   - 每个字段包含 `value`、`confidence`、`evidence`、`updated_at`。
   - 用于个性化解释、资源推荐和学习路径规划。

4. 学习行为记忆 `LearningActivityMemory`。
   - 记录阅读进度、停留时间、笔记、高亮、收藏、资源点击、练习提交。
   - 用于判断真实学习行为，而不是只依赖用户自述。

5. 知识掌握记忆 `MasteryMemory`。
   - 以知识点为单位记录掌握度。
   - 每个知识点包含掌握分、错题次数、最近练习结果、相关资料阅读状态。
   - 用于路径调整、题目推荐和复习提醒。

6. 情景记忆 `EpisodicMemory`。
   - 记录重要学习事件。
   - 例如“用户在学习反向传播时连续两次提问链式法则”“用户完成了鸢尾花分类实验但不会解释准确率”。
   - 可用向量检索，帮助主 Agent 找回类似历史情景。

7. 偏好记忆 `PreferenceMemory`。
   - 记录用户喜欢的讲解方式、资源形式和交互风格。
   - 例如偏好图解、类比、代码案例、短视频脚本、先给结论再展开。

### 类 Hermes 的记忆处理流程

每次用户输入后，主 Agent 不应该直接把所有历史塞进提示词，而是执行记忆检索与压缩流程：

```text
用户输入
  ↓
主 Agent 识别意图和上下文
  ↓
Memory Service 检索相关记忆
  ├─ 当前课程/资料上下文
  ├─ 相关对话摘要
  ├─ 学生画像字段
  ├─ 知识点掌握度
  └─ 相似历史学习事件
  ↓
组装最小必要上下文
  ↓
Planner 拆任务
  ↓
专家 Agent 执行
  ↓
主 Agent 总结结果
  ↓
Memory Writer 判断是否写入/更新记忆
```

### 记忆写入机制

需要单独设计 `Memory Writer`，不能每一句话都写入长期记忆。

可写入长期记忆的情况：

- 用户明确表达稳定偏好，例如“我更喜欢用代码理解”。
- 用户暴露明显薄弱点，例如连续问同一知识点。
- 用户完成练习或测评，产生掌握度证据。
- 用户在资料中高亮、笔记、收藏或标记“不懂”。
- 用户完成某个学习路径节点。

不应写入长期记忆的情况：

- 临时闲聊。
- 一次性的表达。
- 没有证据支撑的模型推测。
- 已经过期或被新证据推翻的信息。

### 记忆字段设计示例

```json
{
  "student_id": "s001",
  "memory_type": "weakness",
  "key": "监督学习流程",
  "value": "用户容易混淆训练集、测试集和模型评估指标",
  "confidence": 0.82,
  "evidence": [
    {
      "source_type": "conversation",
      "source_id": "msg_1024",
      "summary": "用户询问为什么训练准确率高但测试准确率低"
    },
    {
      "source_type": "exercise",
      "source_id": "quiz_33",
      "summary": "模型评估题答错"
    }
  ],
  "updated_at": "2026-06-07T21:44:00+08:00",
  "decay_policy": "weaken_if_mastery_improves"
}
```

### 主 Agent 使用记忆的方式

主 Agent 每次回答前，应只取和当前任务相关的记忆：

- 如果用户在阅读资料中提问，优先取当前资料上下文、相关知识点掌握度、最近困惑。
- 如果用户要求规划学习，优先取学习目标、掌握度、可用时间、历史学习行为。
- 如果用户要求生成练习，优先取薄弱点、已做过题目、题目难度偏好。
- 如果用户只是问概念解释，优先取讲解偏好和当前课程资料证据。

### 与专家 Agent 的关系

主 Agent 从 Memory Service 中取出必要记忆，再注入给 Planner 和专家 Agent。专家 Agent 不直接拥有长期记忆，但可以读取主 Agent 传入的记忆摘要。

例如练习题 Agent 收到的不是完整学生历史，而是：

```json
{
  "topic": "监督学习",
  "target_difficulty": "medium",
  "weaknesses": ["训练集和测试集混淆", "评估指标理解不足"],
  "avoid_repeated_questions": ["q12", "q15"],
  "preferred_style": "结合代码案例"
}
```

### 结论

主 Agent 记忆应借鉴 Hermes 式的长期记忆思想，但工程实现上应采用“主 Agent + Memory Service + Memory Writer + 分层记忆库”的架构。这样既能保留个性化学习的连续性，又不会让上下文无限膨胀，也方便审计、更新和遗忘。

## 2026-06-07 主 Agent 记忆简化方案

### 用户修正

上一版记忆类型过多，工程实现会变复杂。记忆系统应收敛为少数几个清晰类别：

1. `user_profile`
2. 工作记忆
3. 对话记忆
4. `memory`
5. `me`

### 最终记忆分类

1. `user_profile`
   - 用户画像，是结构化、可更新的长期信息。
   - 合并用户基本信息、近期学习内容、学习行为、知识掌握、薄弱点、常错点和偏好信息。
   - 包含专业、课程、学习目标、知识基础、近期正在学习的内容、薄弱知识点、经常错误的题型或概念、知识点掌握度、阅读行为、练习表现、资源偏好、讲解偏好等。
   - 每个重要字段应包含 `value`、`confidence`、`evidence`、`updated_at`。

2. 工作记忆 `working_memory`
   - 当前这一次任务或会话中的临时上下文。
   - 包括当前页面、当前课程、当前资料、选中文本、用户本轮问题、Planner 任务计划、专家 Agent 输出草稿。
   - 生命周期很短，任务结束后不直接长期保存，只能由 Memory Writer 选择性写入 `user_profile`、对话记忆或 `memory`。

3. 对话记忆 `conversation_memory`
   - 每天的对话记录保存为一个 JSON 文件，以日期命名。
   - 例如 `conversations/2026-06-07.json`。
   - 保存当天原始对话、摘要、涉及课程、涉及知识点、未解决问题和当天学习结论。
   - 原始对话用于追溯，摘要用于后续检索和上下文压缩。

4. `memory`
   - 重要长期记忆。
   - 记录关键事件、长期偏好、用户明确要求记住的事情、踩过的坑、重要学习事件。
   - 例如“用户在学习反向传播时连续两次提问链式法则”“用户要求以后解释概念时先给例子”“用户之前踩过环境变量配置的坑”。
   - 这类记忆不一定属于某个知识点，但会影响后续交互。

5. `me`
   - Agent 自身设定和交互风格。
   - 包括 Agent 的角色、人设、回复方式、语气、边界、是否主动追问、是否简洁、是否适合比赛辅导场景。
   - 这不是用户记忆，而是主 Agent 的自我配置。

### 对话记忆 JSON 设计

每天一个文件：

```json
{
  "date": "2026-06-07",
  "student_id": "s001",
  "messages": [
    {
      "time": "21:49",
      "role": "user",
      "content": "监督学习和测试集为什么要分开？"
    },
    {
      "time": "21:50",
      "role": "agent",
      "content": "因为测试集要模拟未见数据，用于评估泛化能力。"
    }
  ],
  "summary": "今天主要讨论监督学习流程、训练集和测试集划分、模型泛化。",
  "topics": ["监督学习", "模型评估"],
  "open_questions": ["用户还不完全理解过拟合和泛化的关系"],
  "memory_candidates": [
    {
      "type": "weakness",
      "content": "用户容易混淆训练准确率和测试准确率",
      "confidence": 0.78
    }
  ]
}
```

### 记忆压缩机制

记忆必须有压缩机制，不能无限把历史塞进上下文。

1. 对话级压缩。
   - 每次对话结束后，更新当天 JSON 的 `summary`。
   - 摘要只保留学习目标、关键问题、已解决内容、未解决问题、可能写入长期记忆的候选项。

2. 天级压缩。
   - 每天结束或第二天首次启动时，将当天对话压缩成 `daily_summary`。
   - 原始 messages 仍保留，但默认不进入 Agent 上下文。

3. 周期压缩。
   - 每周或每若干天，把多个 `daily_summary` 合并成阶段总结。
   - 阶段总结用于更新 `user_profile` 中的薄弱点、掌握度和偏好。

4. 长期记忆压缩。
   - `memory` 中相似记忆需要合并。
   - 过期、被新证据推翻、置信度低的记忆要降权或归档。

### 记忆搜索机制

主 Agent 每次回复前不读取全部记忆，而是搜索相关记忆。

搜索来源：

- `user_profile`：按字段直接读取，例如薄弱点、偏好、掌握度。
- `conversation_memory`：按日期、课程、知识点、关键词、摘要检索。
- `memory`：按语义相似度和标签检索重要长期记忆。
- `me`：默认读取基础人设和回复约束。

搜索方式：

1. 结构化检索。
   - 用于 `user_profile` 和 `me`。
   - 例如直接读取 `learning_preferences`、`weak_points`、`response_style`。

2. 关键词检索。
   - 用于按课程、知识点、日期找历史对话。

3. 向量检索。
   - 用于从 `memory` 和对话摘要中找相似历史事件。

4. 时间权重。
   - 最近记忆优先。
   - 但用户明确要求记住的内容优先级高于普通最近对话。

### 每次回复携带多少记忆

每次回复应控制记忆预算，避免上下文过长和无关信息干扰。

推荐默认携带：

1. `me`
   - 始终携带精简版。
   - 约 300-500 字。

2. `user_profile`
   - 携带和当前任务相关的字段。
   - 约 500-1000 字。
   - 例如当前问题是出题，只带薄弱点、掌握度、题目偏好、已做题记录。

3. 工作记忆
   - 始终携带当前任务必要上下文。
   - 包括当前资料片段、选中文本、页面位置、Planner 任务。
   - 约 1000-3000 字，视资料片段长度调整。

4. 对话记忆
   - 默认携带最近 3-5 轮对话摘要。
   - 如果问题涉及历史，再检索相关日期摘要。
   - 约 500-1000 字。

5. `memory`
   - 默认只携带 Top 3-5 条最相关的重要记忆。
   - 每条记忆应短小，包含内容、来源、置信度。
   - 约 300-800 字。

### 记忆注入顺序

主 Agent 每次回复前的上下文组装顺序：

```text
1. me：我是谁、怎么回复、边界是什么
2. user_profile：这个用户是谁、当前学习状态如何
3. working_memory：当前任务现场，包括页面、资料、选中文本、Planner 任务和专家输出草稿
4. recent_conversation：最近 3-5 轮对话
5. retrieved_conversation：按需检索到的历史日期对话摘要
6. retrieved_memory：重要长期记忆
7. course_context：课程资料和 RAG 检索结果
8. user_message：用户当前输入
```

### 写入规则

不是所有信息都写入长期记忆。

写入 `user_profile`：

- 学生基础、目标、偏好、近期学习内容、薄弱知识点、常错点、掌握度、阅读行为、练习表现发生变化。

写入 `conversation_memory`：

- 所有日常对话都进入当天 JSON 文件。
- 同时更新当天摘要。

写入 `memory`：

- 用户明确说“记住这个”。
- 出现重要学习事件。
- 发生明显踩坑或约定。
- 某个偏好长期稳定出现。

写入 `me`：

- 只有系统设计者或用户明确要求改变 Agent 人设、回复方式、交互边界时才写入。

### 结论

主 Agent 记忆系统最终采用简化设计：

```text
me
user_profile
working_memory
conversation_memory（每天一个 JSON）
memory（重要长期事件）
```

同时必须具备压缩、搜索和记忆预算控制。主 Agent 每次只携带和当前任务相关的少量记忆，而不是加载全部历史。

